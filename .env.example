
# Server configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
# IMPORTANT: In production, use a secure, long, random string (min 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secure-jwt-secret-here

# Supabase Configuration
# Use the URL and service role key (not anon key) from your Supabase project settings
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# Twilio Configuration (for OTP)
# Optional in development mode (OTPs will be logged to console)
# Required in production for sending actual SMS
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=your-twilio-phone-number

# Note: In production:
# 1. Set NODE_ENV=production
# 2. Use a secure secret key generator for JWT_SECRET
# 3. Ensure all API keys and secrets are properly secured
# 4. Consider using environment variable management solutions
